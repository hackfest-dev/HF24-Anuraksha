import React from "react";
import Sidebar from "../components/SideBar";

const Inbox = () => {
    return (
        <section>
            <Sidebar />
        </section>
    );
};

export default Inbox;
